RELAY_URL_A=https://relay-a.example.com
RELAY_URL_B=https://relay-b.example.com
# Replace the placeholders above before starting the Agent. For one dedicated
# relay, set RELAY_URL_A=https://relay.example.com and RELAY_URL_B= (or omit B).
# Two distinct relays in separate failure domains remain recommended.
