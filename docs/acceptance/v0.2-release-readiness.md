# v0.2.0 Release Readiness

Closeout acceptance for the P0 fleet-upgrade series, evaluated as one
releasable whole. The verdict below is a recommendation only: it does not
create the `v0.2.0` tag, the GitHub Release, or any production asset.

## Candidate

- Frozen release-relevant candidate SHA:
  `180cca04b89885085cfff8a8e40c2ab4b6c3d4d8`
- Branch: `codex/p0-closeout-v0.2-release-readiness` (PR #122)
- Base: `ed3267e114fd8abdae5b87f444065a6b69f038c9` = `origin/main`
- Baseline release: v0.1.1 (published 2026-08-26)
- Target version: 0.2.0
- Acceptance report: this documentation-only commit on top of the frozen
  release-relevant candidate; the release-relevant tree is identical to
  `180cca04b89885085cfff8a8e40c2ab4b6c3d4d8`.

Freeze scope: an earlier evidence set was validated at
`334caeaae1e1ec7740e30f8c50672e9e65cb62dd` and superseded at
`c64ca999098b7bbefacd4905b32f5fff9efa0958` (commit `c64ca99` aligning the F3
test fixture with the shipped upgrade state directory contract). The only
diff from that tree to the frozen candidate is
`scripts/release-baseline-upgrade-smoke.sh` (commit `180cca0`, pinning the
published v0.1.1 manifest identity after review: the v0.1.1 GitHub release is
not immutable, so the smoke now pins the exact `SHA256SUMS` digest and fails
closed for unpinned baselines). No product, workflow, migration, or web file
changed. Every release-path gate below is bound to the frozen candidate; the
retained rows are explicitly marked with the tree they executed on and why
that tree is identical for their path.

## P0 implementation status

| # | PR | Feature | Status |
| --- | --- | --- | --- |
| P0-01 | #106 | Web Information Architecture | merged |
| P0-02 | #108 | Real Operational Overview | merged |
| P0-03 | #109 | Agent Version Intelligence | merged |
| P0-04 | #110 | Typed AgentUpgrade Contract | merged |
| P0-05 | #111 | Durable Standalone Upgrade Runner | merged |
| P0-06 | #112 | Single-node Upgrade + Reconciliation | merged |
| P0-07 | #121 | Fleet Canary / Rolling Upgrade | merged |

## Acceptance gates

| Gate | Result | Evidence |
| --- | --- | --- |
| Repository validation | PASS | CI run 33264861716 on the frozen candidate (three required aggregate jobs: policy/docs/contracts, Go+PostgreSQL 17/18+local slice, Rust+native fixtures+browser E2E+P1 smoke+security/license; attempt 2 — the first attempt's tool bootstrap hit a transient download 504, unrelated to the diff) |
| DB migrations | PASS | PostgreSQL 17/18 Integration workers green in CI run 33264861716 on the frozen candidate; additionally `scripts/database-integration.sh` executed locally at tree `334caea` (fresh migrate, the 28→26 down-chain with rollback-guard rows, re-upgrade, idempotent re-run, unknown-schema probes, and the upgrade/rollout/authorization `-run Integration` suites on both majors); the migration and Go tree is identical in the frozen candidate |
| amd64 package | PASS | Release Packages dry-run run 33264860323 on the frozen candidate (build, sign, package, lifecycle smoke, baseline upgrade smoke, validation) |
| arm64 package | PASS | Release Packages dry-run run 33264860323 on the native `ubuntu-24.04-arm` runner |
| deb lifecycle | PASS | Release dry-run native smoke in run 33264860323: fresh install, upgrade, state preservation, removal preserving identity/config, no automatic enable |
| rpm lifecycle | PASS | Release dry-run native smoke in run 33264860323 inside a systemd Rocky Linux 9 container: install, upgrade, snapshot, erase preserving state |
| tar lifecycle | PASS | `scripts/validate-release-packages.sh` in run 33264860323: embedded-triple digest equality, trust anchor/fingerprint consistency, arch checks, SHA256SUMS coverage |
| Published baseline upgrade (v0.1.1 → 0.2.0) | PASS | `release-baseline-upgrade-smoke.sh` in run 33264860323 on both architectures: the downloaded `SHA256SUMS` first verified byte-for-byte against the in-repo pinned v0.1.1 digest (`SHA256SUMS: OK` in the run log), then the manifest signature and the published v0.1.1 deb digest verified, the baseline installed, and the upgrade to the 0.2.0 candidate asserted state, snapshot, and no-enable |
| Upgrade trust boundary | PASS | `AgentUpgrade` wire message carries only `target_version`, `package_sha256`, `architecture`; no URL/path/executable/command/args/shell/environment field exists; the API request takes only target version, approval, and reason; digests resolve exclusively from `internal/releasecatalog` (operator-provisioned manifest, fail-closed startup, exact `(version, architecture)` lookup); the runner reads only the fixed spool and pinned trust anchors; compiled and tested by CI run 33264861716 on the frozen candidate |
| v2 upgrade fence | PASS | Capability `ocserv.agent.upgrade.v2` identical across Go (`commandauth`, `operations`, `telemetry`) and Rust (`contracts`, `agent`, `privd`); shared cross-language corpus `testdata/agent-upgrade-strict-upgrade-v1.json` (SemVer 2.0.0 precedence incl. u64 boundaries and big numerics) drives both implementations; execution-time fence test `runner_refuses_a_target_not_newer_than_the_running_release`; Rust validation green in CI run 33264861716 on the frozen candidate |
| Durable upgrader | PASS | 28 Rust upgrader tests executed locally at tree `334caea` (`cargo test --release -p ocservia-upgrader -p ocservia-contracts`): immutable intent, exact-replay convergence, conflicting-identity rejection, single-active-operation lock, crash windows (intent committed before trigger, runner crash, both binaries installed without commit record, agent binary alone), spool digest/architecture/malformed-identity refusals, unsafe durable ancestry rejection; the Rust tree is identical in the frozen candidate and re-tested by CI run 33264861716 |
| Single-node upgrade | PASS | `TestAgentUpgradeRouteResolvesTrustedReleasesIntegration` and `TestAgentUpgradeReconciliationLifecycleIntegration` against real PostgreSQL in CI run 33264861716 on the frozen candidate; success requires the durable local result, node online, and a fresh target-version observation — schedule/ack/disconnect are never success |
| Rollback | PASS | F3 run 33261745570 at tree `c64ca99` (`package_upgrade_rollback=PASS`): rollback substitution refusals (digest replacement, symlink, owner, mode, hard link) plus matched snapshot restore and durable-runner preservation in `scripts/i18-agent-package-smoke.sh`; the F3 path never executes the release baseline smoke, so the c64ca99 → frozen delta does not touch it; `rolled_back` marking of non-terminal operations covered by the operations integration tests in CI run 33264861716 |
| Controller restart | PASS | Outbox/claim/lease recovery covered by the coordination, connectionowner, and ownersession integration suites against PostgreSQL 17 and 18 in CI run 33264861716 on the frozen candidate; rollout rows and node operations are durable state advanced under row locks, so a restart resumes rather than duplicates |
| Canary rollout | PASS | `TestAgentRolloutFleetLifecycleIntegration` (canary + batches + resume) against real PostgreSQL in CI run 33264861716 on the frozen candidate; exactly one canary node, bounded later batches, stable operation IDs, succeeding nodes never redispatched |
| Canary failure | PASS | Resume semantics in `ResumeAgentRollout`: only failed/unknown/rolled-back nodes and a skipped canary are requeued — no operator path can bypass an unsuccessful canary; covered by the rollout integration suites in CI run 33264861716 |
| Dispatch eligibility race | PASS | `TestRolloutDispatchRechecksEligibilityAfterClaimIntegration` in CI run 33264861716: eligibility is rechecked after the dispatch claim (offline, stale, capability, version, active upgrade, manifest), the node is skipped with a reason, and the rollout pauses |
| Rollout success semantics | PASS | The rollout detail view lists succeeded/failed/rolled-back/unknown per batch, labels `skipped` nodes with their reason, and shows the remaining count, so `succeeded` is never presented as "every node upgraded"; web unit and browser E2E suites green in CI run 33264861716 |
| RBAC / workspace / approval | PASS | `agent.upgrade` is an Operator action distinct from `operation.read`; rollout routes resolve their workspace and authorize the action; single-node approvals bind the exact release identity and rollout approvals bind target version + sorted node set + batch size + stop-on-failure (`TestAgentUpgradeBindingBindsExactReleaseIdentity`, two-person approval and single-consumption tests), all in CI run 33264861716 |
| Browser E2E | PASS | Playwright suite in CI run 33264861716 (desktop and mobile projects): fleet versions, reconciled upgrade lifecycle, forced failure, ineligible node, canary/batch rollout, pause and resume, workspace switching |
| Cross-VM Real E2E | PASS | Run 33258580914 at tree `334caea`: real Controller VM + Managed Node VM, real transportd and Iroh, EndpointID exchange, one-time token enrollment; the real-E2E path is identical in the frozen candidate (the later branch commits touch only the F3 fixture script and the release baseline smoke) |
| G6 smoke | PASS | G6 Harness Smoke run 33264861775 on the frozen candidate (regression smoke only, engineering authority) |
| Formal G6 | PASS | G6 Readiness Harness run 33264861839 on the frozen candidate, `authority=production_readiness`: multi-failure-domain topology, ≥50 agents, 300-second window, independent verification recomputed from raw artifacts, verdict bound to the candidate SHA (see release note below) |
| Release dry-run | PASS | Release Packages `workflow_dispatch` version=0.2.0 run 33264860323 on the frozen candidate: dry-run only (no tag, no release, no published assets; ephemeral signing key — production signing credentials are provisioned separately and were not exercised) |
| F3 systemd / privd / PKI | PASS | `p1-capacity.yml` phase `f3-systemd-privd-pki` run 33261745570 at tree `c64ca99`: near-production privd unit under the shipped sandbox contract, restart stability, attestation-key fail-closed, PKI registration, and the native package upgrade/rollback smoke (`F3=PASS`, `package_upgrade_rollback=PASS` in the run artifact); the F3 path is identical in the frozen candidate (the c64ca99 → frozen delta is the release baseline smoke, which F3 never executes). The first dispatch (run 33259931124) failed with `status=226/NAMESPACE` on `/var/lib/ocservia-upgrade` — test fixture drift, not a product defect: the fixture's `systemd-run` mirrors the shipped `ReadWritePaths`, real package installs create that directory, the fixture did not; commit `c64ca99` aligns the fixture |
| Bounded soak | OBSERVATION | The P1 smoke (24 agents, bounded heartbeats and request concurrency) and the G6 300-second fault-free window run green on the frozen candidate (CI run 33264861716, G6 run 33264861839); no runaway retry, queue growth, or reconnect storm was observed. Elapsed-time soak is not release-blocking by repository policy. |

## Known non-blocking issues

- `shellcheck` 0.11 (current Homebrew) flags pre-existing warnings in
  `scripts/g6-checkpoint-secret-policy.sh`, `scripts/i18-agent-package-smoke.sh`,
  and `scripts/test-g6-ha-pitr-workflow.sh`; the pinned CI toolchain passes.
  The shellcheck version is not pinned in `toolchains.lock`.
- The rpm leg of the published-baseline upgrade smoke is not implemented
  (the deb leg runs on both release architectures); the rpm cross-version
  upgrade remains covered by the fabricated-version smoke and the native
  package scriptlets are architecture-independent.
- The rollout batch header aggregates failed counts but not a separate
  skipped count; per-node skipped states and reasons are displayed.

## Release blockers

- NONE at the frozen candidate.

## Final verdict

READY FOR v0.2.0.

The frozen candidate satisfies the repository's v0.2 engineering
release-readiness gates: required CI on the exact SHA, migrations on both
PostgreSQL majors, the full package matrix including the real v0.1.1 → 0.2.0
baseline hop pinned to the historical v0.1.1 manifest identity on amd64 and
arm64, the upgrade trust boundary and v2 fence, the durable upgrader crash
windows, real F3 systemd/privd/PKI evidence, real cross-VM enrollment,
browser E2E, and the formal `production_readiness` G6 verdict on the
candidate.

This verdict is a recommendation only. It does not create the `v0.2.0` tag,
the GitHub Release, or any production release asset; publishing remains a
separate, manual action.

G6 `production_readiness` evidence is bound to the run's candidate commit
SHA, so the release procedure must verify or rerun the authoritative
production_readiness gate against the final tagged commit before publishing
assets.
