# ocserv-dashboard v4.9 post-release backport

## Pinned comparison

- Repository: `mmtaee/ocserv-dashboard`
- Branch: `master`
- Imported at: `2026-08-07T16:41:52Z`
- Old ref: `v4.9`
- Old commit: `b8f59026c4d879f40c1da43dc00d97e34f9790bc`
- New ref: `master`
- New commit: `4d25478580d899b77460bdf0cf0a590cfdd26030`
- License: MIT

The machine-readable pinned inventory and A/B/C/D decision are in
`v4.9-post1.manifest.json`. The vendored comparison artifact
`v4.9-post1.patch` is digest-checked by the implementation CI; the GitHub
comparison reports two commits, one changed file, and the same MIT license blob
at both pinned commits.

The exact upstream diff is one change in
`web/src/components/auth/SetupForm.vue`: numeric form binding replaces string
binding for the inactive-user day value. Directory, dependency, and migration
inventories otherwise have no old-to-new change. The
authentication setup form is security-sensitive because it configures inactive
user deletion retention, so the inventory explicitly includes it for review.

## Classification

- A: no files in the exact comparison.
- B: the numeric form correction is represented at the adapter boundary. The
  upstream-facing policy fields stay in `web/src/upstream/`; node scope,
  generated API calls, integer-byte conversion, UTC normalization, operation
  behavior, and RBAC stay in `web/src/adapters/` and the control plane.
- C: v4.9 quota/expiry product semantics were manually mapped to integer bytes,
  explicit receive/transmit/combined modes, UTC periods, desired policy,
  observed usage, a leased scheduler, and typed disable operations. Upstream
  GORM models and migrations are not executed.
- D: local `occtl`, local password/config file access, Docker-mode execution,
  the file cron journal, unconstrained goroutines, and permanent expired-user
  deletion are rejected.

No upstream source file is copied verbatim. This standalone backport publication
contains only the pinned comparison, classification decision, notice, and
review artifact. The adapted product implementation is isolated in I14 and is
validated there by contract, permission, migration, generated-client, UI, E2E,
security, and license checks.

## Rollback

Revert this standalone backport commit to remove the comparison record,
classification, and notice without changing runtime behavior. The I14
implementation has its own rollback boundary: stop its scheduler/API version,
allow existing node command leases to reach a durable state, revert I14, and
apply migration `000013` down only when no policy or batch data must be retained.
No upstream runtime, database, container, or privileged deployment dependency
is introduced.
